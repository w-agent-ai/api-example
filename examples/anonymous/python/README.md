# Anonymous x402 Python Demo Bundle

This bundle contains anonymous x402 Python demos. Single-algorithm packages include algorithm-specific English and Chinese README files.

Before running a script, open the corresponding `.py` file and edit `EVM_PRIVATE_KEY` and the input path settings.
