# Go Human 2D/3D Keypoints API Demo

This package only demonstrates human 2D/3D keypoint extraction for registered users.

Put one person's ordered frame images under `./images`, edit `apiKey` in `gait_pose_demo/main.go`, then run the Go example.
