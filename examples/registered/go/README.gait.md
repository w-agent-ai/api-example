# Go Gait Recognition API Demo

This package only demonstrates gait recognition for registered users.

Put one person's ordered frame images under `./images`, edit `apiKey` in `sequence_demo/main.go`, then run the Go example.
