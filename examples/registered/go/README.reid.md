# Go ReID API Demo

This package only demonstrates ReID feature extraction for registered users.

Edit `apiKey` in `reid_feature_demo/main.go`, then run:

```bash
go run ./reid_feature_demo /path/to/person.jpg
```
