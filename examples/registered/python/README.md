# Registered Python Demo Bundle

This bundle contains registered-user Python demos. Single-algorithm packages include algorithm-specific English and Chinese README files.

Before running a script, open the corresponding `.py` file and edit `API_KEY` and the input path settings.
