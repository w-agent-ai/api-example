# Registered Python Demo Bundle

This bundle contains multiple registered-user Python demos. Single-algorithm packages include algorithm-specific English and Chinese README files.

Before running a script, open the corresponding `.py` file and edit the top-level `API_KEY` and input path settings.
