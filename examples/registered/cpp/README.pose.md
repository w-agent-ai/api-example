# C++ Human 2D/3D Keypoints API Demo

This package only demonstrates human 2D/3D keypoint extraction for registered users. It supports two inputs:

1. Existing tracked person sequence images.
2. A local video, processed locally into person sequences before calling the API.

Edit `kAPIKey` in the relevant `main.cpp` before building.
